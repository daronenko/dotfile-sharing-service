<p style="color: green"><%= notice %></p>

<%= render @dotfile %>

<div>
  <% if @dotfile.user == current_user %>
    <%= link_to "Edit this dotfile", edit_dotfile_path(@dotfile) %>
    <%= button_to "Destroy this dotfile", @dotfile, method: :delete %>
  <% end %>

    <%= link_to "Back to dotfiles", dotfiles_path %>
</div>
